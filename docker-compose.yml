services:
  postgres:
    image: postgres:16
    container_name: gametracker-postgres
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - gametracker_pgdata:/var/lib/postgresql/data


volumes:
  gametracker_pgdata:
